import * as Styled from './styled';
import * as GlobalStyle from '../../styles/global';

const Photos = () => {
    return (
        <Styled.LoginContainer>
            <GlobalStyle.ContainerGlobal>Photos</GlobalStyle.ContainerGlobal>
        </Styled.LoginContainer>
    );
};

export default Photos;
