import * as Styled from './styled';
import * as GlobalStyle from '../../styles/global';

const Register = () => {
    alert();
    return (
        <Styled.LoginContainer>
            <GlobalStyle.ContainerGlobal>Register</GlobalStyle.ContainerGlobal>
        </Styled.LoginContainer>
    );
};

export default Register;
