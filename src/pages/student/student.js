import * as Styled from './styled';
import * as GlobalStyle from '../../styles/global';

const Student = () => {
    return (
        <Styled.LoginContainer>
            <GlobalStyle.ContainerGlobal>Student</GlobalStyle.ContainerGlobal>
        </Styled.LoginContainer>
    );
};

export default Student;
